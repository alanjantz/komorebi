name: GitHub Pages Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:      
      - name: Checkout
        uses: actions/checkout@v2

      - name: Deploy
        uses: xmflsct/action-publish-gatsby@v1.0.0
        with:
          publishBranch: 'gh-pages'
          token: ${{ secrets.TOKEN }}
          name: 'Alan Jantz'
          email: 'alan_f_jantz@hotmail.com'
