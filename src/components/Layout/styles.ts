import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#171e28',
    },
    secondary: {
      main: '#f58d1e',
    },
    background: {
      default: '#eff1f4',
    },
  },
});

export { theme };
