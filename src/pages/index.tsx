import React from 'react';
import { PageProps } from 'gatsby';
import Layout from 'components/Layout';

const Home: React.FC<PageProps> = () => (
  <main>
    <Layout>Olá, tudo bem?</Layout>
  </main>
);

export default Home;
